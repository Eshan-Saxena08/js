//control/logic flow is that not the whole program must be run .... program must be run as per the current stage EG.logged in or off

//if
if(true){
    //code
}
//if-else
    const temp = 42
    if(temp === 42){//=== is same as == in java ,but == in JS doesnt check data type
        console.log("less than 50");
    }else{
        console.log("More")
    }


    //var is global

    if(temp>50) console.log("test")//implicit scope

    //else-if
    if(true){

    }else if(true){

    }else{

    }

    //switchcase
    const month = 3//can be string also
    switch(month){
        case 1:
            break;
        case 2:
            break;
        default:
            break;
    }
    